namespace Sendloop.Enum {
    /// <summary>
    /// Set the subscription type. It can be opt-in (no email confirmation will be sent) or double opt-in (confirmation email will be sent - recommended).
    /// </summary>
    public enum OptInModeType {
        Single,
        Double
    }
}
